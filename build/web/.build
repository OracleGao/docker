export DOCKER_IMAGE_BUILD=node:8.15.0-alpine

#export DOCKER_REGISTRY=${DOCKER_REGISTRY:-cw-registry:5000}
export CURRENT_PATH=$(pwd)
export SERVICE_NAME=${CURRENT_PATH##*/}
#export IMAGE=${DOCKER_REGISTRY}/${SERVICE_NAME}:${SERVICE_VERSION}
